# copy to .env.local for local dev. on Vercel, set these in
# Project → Settings → Environment Variables.
#
# the live instrument now lives in the shared INSTRUMENTS Supabase project
# (ref: lueovxcoqrkjdjxfvwng) alongside TOMI NIASHI. all of our tables and
# buckets are prefixed `li_` / `li-` to keep them separate (see src/supa.js).
VITE_SUPABASE_URL=https://lueovxcoqrkjdjxfvwng.supabase.co
VITE_SUPABASE_ANON_KEY=
